#include <iostream>

using namespace std;

int main(){
	int a,b;
 	cin >> a >> b;
  	if(a%2==0 || b%2==1){
    	cout << "ERROR" << endl; 
    }else{
     	if(a>=b) cout << a << endl;
      	else cout << b << endl;
    }
  	return 0;
}
